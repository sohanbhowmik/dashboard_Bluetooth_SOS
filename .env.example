# Copy this file to ".env" and fill in your real MongoDB Atlas credentials.
# Never commit the actual ".env" file — only this example file should go in git.

MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority